import React from 'react'
import { isEmpty } from 'lodash';
import Button from '../../pages/Dashboard/button';

const ImgUpload = ({ images, handleRemoveImg, ref, handleClick, onChangeFile, readOnly }) => {
    return (
        <>

        </>
    )
}

export default ImgUpload;
